package OneLang.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class LoB_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<LoB> constants = ListSequence.fromList(LoB.getConstants()).iterator();
    while (constants.hasNext()) {
      LoB constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<LoB> constants = ListSequence.fromList(LoB.getConstants()).iterator();
    while (constants.hasNext()) {
      LoB constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    LoB constant = LoB.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "N/A";
  }
}
